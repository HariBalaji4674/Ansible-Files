# Password less authentication --> By adding the ssh configuration

ssh-keygen --> generate the ssh keys for all the servers and add host/ansible-server public key to the all the servers

Ansible Playbook:
Ansible Inventory File


ansible-galaxy roles init kubernetes --> it will give the roles folder/directory structure

ansible-galaxy will create 
    templates
    roles
    variable
    handlers
    tests
    defaults
    meta
*** Create Ec2 Instances and Configue one those as master-node/master-node and two of those as Worker nodes/Control Nodes ***

