Ansible Intro :

Disadvantages of shell scripting:
    One shell script is work for one distributions
    Shell Script is homogeneous
    Manual Validation should be write --> Functions also need to write 
    imperative hard to remember
    Everytime we need to run the sript we need to log in everytime 
    shell script is imperative : Follow Strict sequence and syntax is somewhat difficult
    It is hard to read.

Ansible Advantages:
    It is Declarative in nature and written in python language
    YAML format of sytax and easy syntax building
    It will query the server and understand which os it is change the commands based on that 
    no need to write manual validations we can add modules
    can connect any number of servers
    Push Machanism feature 
    
